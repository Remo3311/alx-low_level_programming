#include <stdio.h>

/**
 * main - prints program name, followed by a new line
 * @argc: int
 * @argv: list
 * return: 0
 */

int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[o]);
	return (0);
}
